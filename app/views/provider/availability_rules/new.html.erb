<h1 class="h4 mb-3">
  Ajouter une disponibilité récurrente pour <%= @service.name %>
</h1>

<%= render "form" %>

<div class="mt-3">
  <%= link_to "← Retour aux règles",
      provider_service_availability_rules_path(@service),
      class: "btn btn-link" %>
</div>
