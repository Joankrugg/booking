<h1 class="h4 mb-3">Réservation confirmée</h1>

<p>Merci <strong><%= @booking.customer_name %></strong> !</p>

<p>
  Vous avez réservé :
  <strong><%= @booking.service.name %></strong><br>
  le
  <%= @booking.start_time.in_time_zone.strftime("%d/%m/%Y") %>
  de
  <%= @booking.start_time.in_time_zone.strftime("%H:%M") %>
  à
  <%= @booking.end_time.in_time_zone.strftime("%H:%M") %>
</p>

<p>
  Un rappel pourra vous être envoyé à <strong><%= @booking.customer_email %></strong>.
</p>

<%= link_to "Retour à la prestation", service_path(@booking.service), class: "btn btn-primary mt-3" %>
